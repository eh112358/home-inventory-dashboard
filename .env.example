# REQUIRED: Copy this file to .env and set values before running
# The application will NOT start without these environment variables set

# Secret key for session encryption (use a long random string)
# Generate one with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=

# Application password (minimum 8 characters)
APP_PASSWORD=
